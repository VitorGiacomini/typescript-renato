import Linguagem from "./modulos";
import { nomes, impressao } from "./modulos";

const lang = new Linguagem('JAVA')

impressao('A melhor lingagem de prog e ? resp '+ lang.nome)

console.log(lang)
console.log(nomes)